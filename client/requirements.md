## Packages
framer-motion | Smooth entry animations for cards
clsx | Utility for conditional class names (standard in shadcn/ui)
tailwind-merge | Utility for merging tailwind classes (standard in shadcn/ui)

## Notes
Strict dark mode application.
API endpoints: GET /api/ideas, POST /api/ideas, DELETE /api/ideas/:id
Font: Inter (sans) and JetBrains Mono (mono) for the brutalist feel.
